from setuptools import setup, find_packages
setup(
    name="httpx_wrap",
    version="1.0.0",
    packages=find_packages(),
    description="Python 下一代 HTTP 客户端，支持异步",
    python_requires=">=3.8",
)
